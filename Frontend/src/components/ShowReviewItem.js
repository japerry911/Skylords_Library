import React from 'react';
import { StyleSheet, View } from 'react-native';
import { Text } from 'native-base';

const ShowReviewItem = () => {
    return (
        <View>
            <Text>Show a Review</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ShowReviewItem;